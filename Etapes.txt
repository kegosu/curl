git init
git add .
git commit -m "msg"


git remote set-url origin https://github.com/<username>/<reponame> 
git push --set-upstream origin master


echo "*.class" >> .gitignore : il va ignorer tout les fichier .class